{
  "private": true,
  "name": "md2html",
  "main": "src/index.js",
  "scripts": {
    "dev": "vbuild --dev",
    "build": "vbuild",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "highlight.js": "^9.9.0",
    "mmark": "^0.1.0"
  },
  "devDependencies": {
    "gh-pages": "^0.12.0",
    "vbuild": "^6.24.14"
  }
}
